.DS_Store
__pycache__/
*.pyc

# The design PSDs are 243 MB (the homepage alone is 230 MB, over GitHub's
# 100 MB limit). They stay on Ethan's machine; tools/export_assets.py reads them.
design/psd/

# Local Claude Code config (preview server).
.claude/
